interface:
  display_name: "Fetch 104 Job Data via Browser"
  short_description: "Fetch clean JSON from 104.com.tw via a real browser session."
  default_prompt: "Use $fetch-104-jobs to pull 104 job listings as JSON by same-origin fetch of 104's API from the open Chrome tab (falls back to XHR capture on 403)."
